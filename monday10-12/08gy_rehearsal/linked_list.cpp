#include <iostream>

struct Node {
  int data;
  Node *next;
};

int main() {
  Node *head = nullptr;
  
  

  //Node first{4, nullptr};
  //Node second{5, nullptr};

  //first.next = &second;

  //Node *ptr = &first;
  //while (ptr != nullptr) {
  //  std::cout << (*ptr).data << '\n';
  //  ptr = (*ptr).next;
  //}
}
