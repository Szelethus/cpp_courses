#include <iostream>
#include <vector>
#include <algorithm>
#include <set>

std::vector<int> generateVector();

int main() {
  std::vector<int> vec = generateVector();

  // (1a) STL algoritmus segítségével keressük meg az első elemet vec-ben,
  // amelynek az értéke 3nál nagyobb!
  
  // (1b) Töröljük ki a vectorból ezt, és a rákövetkező elemet is!

  // (3) Írjunk egy polimorfikus osztályt!
  
  // (4) Írjunk egy int-eket tartalmazó halmazt STL konténer segítségével,
  // mely a 7 szerinti maradék szerint rendez!

  // (5) Mi a különbség az std::remove_if és az std::stable_partition között?
}
